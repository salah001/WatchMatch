// src/config/api.js
// Use environment variables in a real app (e.g., with react-native-config)
// For now, we'll hardcode it here, but aim to replace this.

export const API_BASE_URL = 'http://192.168.1.20:5000'; // Your backend URL